{% extends 'base.html.twig' %}

{% block pageheader %} 
    <h1>PageCategory</h1>
{% endblock %} 

{% block body %}

    <div class="btn-toolbar">
        <div class="btn-group pull-right">
            <a href="{{ path('page_category_index') }}" class="btn btn-default">
                <span class="glyphicon glyphicon-arrow-left"></span> Back
            </a>
            {% if is_granted('ROLE_BLOG_ADMIN') %}

                <a href="{{ path('page_category_edit', { 'id': pageCategory.id }) }}" class="btn btn-default">
                    <span class='glyphicon glyphicon-edit'></span> Edit
                </a>
                <a href="{{ path('page_category_delete', { 'id': pageCategory.id }) }}" class="btn btn-default" data-confirm="Are you sure you want to delete this PageCategory?">
                    <span class='glyphicon glyphicon-remove'></span> Delete
                </a>
            {% endif %}
        </div>
    </div>
    <table class="table table-bordered table-condensed table-hover table-striped">
        <tbody>
            <tr>
                <th>Name</th>
                <td>{{ pageCategory.name }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ pageCategory.label }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ pageCategory.description }}</td>
            </tr>
            <tr>
                <th>Created</th>
                <td>{% if pageCategory.created %}{{ pageCategory.created|date('Y-m-d H:i:s') }}{% endif %}</td>
            </tr>
            <tr>
                <th>Updated</th>
                <td>{% if pageCategory.updated %}{{ pageCategory.updated|date('Y-m-d H:i:s') }}{% endif %}</td>
            </tr>
        </tbody>
    </table>

{% endblock %}
